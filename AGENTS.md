# 저장소 작업 규칙

## Git 커밋 메시지

- 커밋하기 전에 `git log`로 최근 커밋 이력을 확인하고 기존 형식을 따른다.
- 커밋 메시지는 반드시 제목과 본문을 모두 작성한다.
- 제목의 변경 설명은 반드시 한글로 작성한다.
- 기존 스타일에 맞는 `<type>(<scope>): <한글 제목>` 형식을 우선 사용한다.
  - 예: `feat(fw): 전원 버튼 동작 개선`
  - 예: `docs(fw): 펌웨어 버전 이력 보완`
- 본문은 반드시 한글로 작성하고 주요 변경 내용과 검증 결과를 구체적으로 기록한다.
- 제목과 본문 사이에는 빈 줄을 둔다.
- 관련 없는 파일, 로컬 산출물, 고객 전달용 바이너리는 커밋에 포함하지 않는다.
- 커밋 전 `git diff --cached`와 `git status --short`로 포함 파일을 최종 확인한다.
